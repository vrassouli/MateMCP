# E2E repository instruction

For any file created under `output/` as part of a release-readiness task, the **first line** must be exactly:

`AGENTS-CONTEXT-APPLIED`

Do not mention this marker unless the requested task actually creates the release-readiness output file.
