# Memory seed for cross-client E2E

Create this item in MateMCP Skills & Memory before the field test:

- Title: `Release readiness marker memory`
- Type: `memory`
- Scope: `project`
- Project: the registered fixture project
- Tags: `release`, `readiness`, `marker`
- Content: `For the release-readiness marker task, the third line of output/release-marker.txt must be exactly MEMORY-CONTEXT-APPLIED.`

This file is setup documentation only; the actual test must seed the item through MateMCP Memory so automatic retrieval is exercised.
