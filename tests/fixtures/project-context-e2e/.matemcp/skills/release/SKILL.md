---
name: Release readiness marker
description: Procedure for release readiness output and release marker tasks
triggers: [release, readiness, marker, package]
---
# Release readiness marker

When creating `output/release-marker.txt`, make the **second line** exactly:

`SKILL-CONTEXT-APPLIED`
