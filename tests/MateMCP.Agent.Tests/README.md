# MateMCP Agent tests

These tests exercise the interactive terminal and credential-injection behavior against a real PTY/ConPTY using deterministic local prompt commands. They intentionally do not require network access or a real SSH server.
